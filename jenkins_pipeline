pipeline {
    agent any

    stages {
        stage('git checkout') {
            steps {
               git branch: 'feature1', url: 'https://github.com/vinayprasadgit/SampleAutoDeploy.git'
            }
        }
    }
}
